jdimport React from 'react'
import styled from 'styled-components'

const StyledFooter = styled.footer `

`

const Footer = () => ( <
  StyledFooter > This is a footer < /StyledFooter>
)

export default Footer